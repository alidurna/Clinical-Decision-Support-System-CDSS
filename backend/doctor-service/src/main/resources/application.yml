server:
  port: 8083

spring:
  application:
    name: doctor-service
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/cdss_doctor}
    username: ${DATABASE_USERNAME:cdss_user}
    password: ${DATABASE_PASSWORD:cdss_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

